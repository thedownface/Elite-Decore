node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Raw staging photos (WhatsApp exports etc.) — the curated, optimized set
# actually used by the site lives in public/images/. Kept out of git so a
# stray third-party-watermarked source photo never ends up in history.
assets/
